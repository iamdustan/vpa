{
  "name": "vpa-monorepo",
  "private": true,
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "overrides": {
    "vite": "8.0.10"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "commander": "^14.0.3",
    "turbo": "latest",
    "typescript": "latest",
    "vitest": "^4.1.5"
  }
}
